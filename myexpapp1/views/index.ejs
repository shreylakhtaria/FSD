<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
      body {
        font-family: 'Segoe UI', Arial, sans-serif;
        margin: 0;
        padding: 0;
        background: linear-gradient(135deg, #e0eafc 0%, #cfdef3 100%);
        color: #222;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }

      .container {
        background: #fff;
        box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.15);
        border-radius: 16px;
        padding: 40px 32px;
        max-width: 400px;
        width: 100%;
        text-align: center;
        margin: 32px 0;
      }

      h1 {
        color: #2c3e50;
        font-size: 2.5rem;
        margin-bottom: 16px;
        letter-spacing: 1px;
      }

      p {
        font-size: 1.2rem;
        margin-bottom: 32px;
        color: #555;
      }

      .btn {
        display: inline-block;
        padding: 14px 32px;
        background: linear-gradient(90deg, #3498db 0%, #6dd5fa 100%);
        border: none;
        border-radius: 8px;
        cursor: pointer;
        box-shadow: 0 4px 16px rgba(52, 152, 219, 0.15);
        transition: transform 0.2s, box-shadow 0.2s, background 0.3s;
        font-size: 1rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      .btn:hover {
        background: linear-gradient(90deg, #2980b9 0%, #56ccf2 100%);
        transform: translateY(-2px) scale(1.03);
        box-shadow: 0 8px 24px rgba(52, 152, 219, 0.18);
      }

      .btn a {
        color: #fff;
        text-decoration: none;
        font-weight: bold;
        letter-spacing: 1px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1><%= title %></h1>
      <p>Welcome to <%= title %></p>
      <button class="btn"><a href="/about">About Us</a></button>
      <button class="btn"><a href="/add-student">Add Student</a></button>
    </div>
  </body>
</html>
