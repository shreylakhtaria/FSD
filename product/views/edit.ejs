<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Product</title>
</head>
<body>
  <h1>Edit Product</h1>
  <form action="/edit/<%= product._id %>" method="post">
    <div>
      <label for="pname">Name</label>
      <input type="text" id="pname" name="pname" value="<%= product.pname %>" required />
    </div>
    <div>
      <label for="pdetail">Details</label>
      <textarea id="pdetail" name="pdetail" required><%= product.pdetail %></textarea>
    </div>
    <div>
      <label for="pprice">Price</label>
      <input type="number" id="pprice" name="pprice" value="<%= product.pprice %>" step="0.01" required />
    </div>
    <div>
      <button type="submit">Save</button>
      <a href="/display">Cancel</a>
    </div>
  </form>
</body>
</html>
